<div class="header">
<h1><%= @category.name %></h1>
<h2><%= @category.description %></h2>
</div>


  <div class="category">
  <% @products.each do |pro| %>
  <div class= "item">
  <%= link_to "Name: #{pro.name}", product_path(pro.id)%>
  <br />
  <%= "Price: $ #{pro.price}" %>
  <br />
  <div id="add_to_cart"><button> Add to Cart</button></div>
  </div>
  <% end %>
  </div>
<br />
<br />
<%= link_to "Go Back", categories_path %>
<%= link_to "Create New Product", new_product_path %>
<%= link_to "Edit Category", edit_category_path(@category.id)%>
